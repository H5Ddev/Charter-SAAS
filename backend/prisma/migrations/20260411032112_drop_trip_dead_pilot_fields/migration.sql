BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[trips] DROP COLUMN [pilots], [returnFbo], [returnPilots];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
